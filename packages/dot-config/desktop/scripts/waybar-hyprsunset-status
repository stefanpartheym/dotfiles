#!/usr/bin/env sh

status_json=$(sunsetr status --json)

gamma=$(echo "$status_json" | jq -r .current_gamma)
tooltip="Temperature: $(echo "$status_json" | jq -r .current_temp)\nGamma:       $gamma"

cat << EOF
{ "text": "", "tooltip": "$tooltip", "class": "", "percentage": $gamma }
EOF

